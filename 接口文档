域名：www.ordos-edu.com
（例如：http://www.ordos-edu.com/getCourses?pageIndex=1&pageSize=1000）
1.首页
	接口名：getCourses
	参数：pageIndex=1&pageSize=1000
	返回值：code（返回码200为正常）,
			demandc3List（推荐课程）,demandc3ListSize, 
			demandc1List(直播回放), demandc1ListSize, 
			demandList（同步课）, demandListSize, 
			list（直播课）, size,
			 teacherList（名师风采）, teacherSize, 
			rollList（轮播图）, rollListSize, 
			partnersList（合作机构）, partnersListSize
2.搜索页
	接口名：search
	参数：title，search_type
	返回值：code（返回码200为正常）,list,size
